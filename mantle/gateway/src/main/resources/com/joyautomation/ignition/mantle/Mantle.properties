# Every user-visible string the gateway asks Mantle for by key. Ignition resolves these through BundleUtil
# (registered in MantleGatewayHook.setup), and renders an unresolved key as ¿the.key? — so a missing entry
# here is visible on the page rather than silent.
#
# A translation is a sibling file: Mantle_de.properties, Mantle_es.properties, and so on.

Connection.MQTT.name=MQTT Broker
Connection.MQTT.desc=A Sparkplug B host application connected to one MQTT broker.
